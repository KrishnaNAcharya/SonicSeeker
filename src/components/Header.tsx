const Header = () => {
  return (
    <header className="w-full h-16 flex items-center justify-center glass-card border-b border-gray-200 dark:border-gray-700 bg-gradient-to-r from-gray-100 to-gray-300 dark:from-gray-800 dark:to-gray-900">
      <nav className="container mx-auto px-4">
        <h1 className="text-xl font-semibold text-gray-800 dark:text-gray-200">SonicSeeker</h1>
      </nav>
    </header>
  );
};

export default Header;
